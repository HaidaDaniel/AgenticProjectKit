# Task 0113 - Align canonical CLI documentation with parser contracts

State: done
Owner: opencode-ds-v41
Mode: maintenance
Lane: docs
Type: bugfix
Scope: docs,templates,cli-contract
Risk: medium
Parallel: false
Depends on: none
Tags: bugfix

## Goal

Keep canonical APK documentation/examples aligned with the actual CLI parser contracts, fix confirmed review-recording documentation drift, and add bounded regression coverage preventing role/flag vocabulary from diverging.

Confirmed canonical bug: review-result examples must include `--review-run` produced by the prior `apk review --prompt` preparation.

Downstream dogfood exposed confusion between the `execution explain` role `implementation` and the `apk work` role `implement`. Audit canonical examples and add regression coverage so these vocabularies cannot be accidentally interchanged.

Do not assert that a downstream hand-authored document (for example `translator-agent/docs/development-workflow.md`) is generated by APK unless repository evidence proves it.

## Context files

- AGENTS.md
- docs/task-system.md
- docs/cli-commands.md
- README.md
- src/core/templates/exporters/agents.md.hbs
- src/cli/commands/task-state.ts
- src/cli/commands/execution.ts
- src/cli/cli.test.ts

## Files allowed to edit

- docs/task-system.md
- docs/cli-commands.md
- README.md
- src/core/templates/**
- src/cli/cli.test.ts
- docs/progress.md
- dist/**

## Files forbidden to edit

- src/core/execution/**
- src/core/work/**
- src/core/tasks/**
- .github/workflows/**

## Steps

1. Confirm the parser contracts for `execution explain` roles, `apk work` roles, and review-result recording flags.
2. Fix the confirmed review-recording drift so every canonical review-result example includes `--review-run <review-run-id>`.
3. Clarify in docs that `apk review --prompt` prepares and returns a `reviewRunId` that must be supplied when recording the result.
4. Audit canonical `execution explain` and `apk work` examples against the exported role constants and fix any invalid example.
5. Add a bounded regression test guarding canonical examples against parser drift.
6. Run verification.

## Acceptance criteria

- If any APK-owned canonical documentation or template contains an example that records a review result, that specific example must include `--review-run <review-run-id>`.
- Docs explain that `apk review --prompt` prepares and returns a `reviewRunId` that must be supplied when recording the result.
- Canonical `execution explain` examples use only execution roles: `planning`, `implementation`, `review`, `fix`, `documentation`, `triage`, `verification`.
- Canonical `apk work` examples use only worker roles: `implement`, `review`, `fix`, `verify`.
- No test falsely assumes that downstream `translator-agent` documentation is generated by APK unless repository evidence proves it.
- A bounded regression test guards these canonical examples against parser drift.

## Correctness assumptions

- Confirmed: review-result examples must include `--review-run` produced by the prior `apk review --prompt` preparation.
- Downstream dogfood exposed confusion between the `execution explain` role `implementation` and the `apk work` role `implement`; canonical examples must use the correct vocabulary for each command.
- The downstream `translator-agent/docs/development-workflow.md` example was authored during adoption/integration work and is not proven to be canonical APK output.

## Invariants

- No general shell parser, markdown command runner, or documentation execution framework is added.
- Fixes land in canonical docs/templates; CLI parsing contracts are unchanged.
- A regression test must verify canonical APK-owned examples rather than downstream hand-authored documentation.

## Required evidence

- Regression test output plus the corrected canonical example snippets.

## Review questions

- Does the test verify canonical APK-owned examples rather than downstream hand-authored documentation?
- Does each actual review-recording example include `--review-run`, without requiring every document to contain such an example?
- Are `execution explain` roles and `apk work` roles kept distinct in every canonical example?
- Does the guard assert against exported role constants or actual parsers rather than an unrelated string?

## Counterexample searches

- A downstream hand-authored document treated as if it were canonical APK output.
- `execution explain` paired with the `apk work` role `implement`.
- A review example that records a result without `--review-run`.
- README quickstart and `docs/cli-commands.md` list drift.
- An agent executing the canonical example end to end.

## Verification

- `{"id":"typecheck","type":"automated","required":true,"environment":"local","profile":"deterministic","command":"pnpm typecheck"}`
- `{"id":"source-lint","type":"automated","required":true,"environment":"local","profile":"deterministic","command":"pnpm lint"}`
- `{"id":"tests","type":"automated","required":true,"environment":"local","profile":"deterministic","command":"pnpm test"}`
- `{"id":"build","type":"automated","required":true,"environment":"local","profile":"deterministic","command":"pnpm build"}`
- `{"id":"dist-current","type":"automated","required":true,"environment":"static","profile":"deterministic","command":"test -z \"$(git status --porcelain --untracked-files=all -- dist)\""}`
- `{"id":"review-run-examples","type":"automated","required":true,"environment":"static","profile":"deterministic","command":"if grep -rnE 'apk review .*--result' docs/task-system.md README.md docs/cli-commands.md | grep -v -- '--review-run' | grep -q .; then echo 'review-record example missing --review-run'; exit 1; fi"}`
- `{"id":"cli-role-vocabulary","type":"automated","required":true,"environment":"static","profile":"deterministic","command":"if grep -rnE 'execution explain.*--role implement([^a-z]|$)' docs/task-system.md README.md docs/cli-commands.md; then echo 'execution explain must not use the worker role implement'; exit 1; fi"}`
- `{"id":"built-lint","type":"automated","required":true,"environment":"static","profile":"deterministic","command":"node dist/cli/index.js lint"}`
- `{"id":"diff-check","type":"automated","required":true,"environment":"static","profile":"deterministic","command":"git diff --check"}`

## Documentation updates

- docs/task-system.md
- docs/cli-commands.md
- docs/progress.md

## Notes

- Keep the fix narrow.
- The guard must not require every document to contain a review example; it asserts that any review-recording example includes `--review-run`.
- Do not add `adoption` to scope unless repository evidence shows canonical adopt-generated docs contain the affected examples.
